global:
  scrape_interval: 15s  # How often to check targets

scrape_configs:
  - job_name: 'react-app'
    static_configs:
      - targets: ['host.docker.internal:80']   # Change this to your React app container/EC2 IP
